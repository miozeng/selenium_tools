package com.mio.selenium.domain;

public enum SeDoType {

	FetchingPage,getText,click,sendKeys,clear,selectByIndex,selectByVisibleText,selectByValue,isSelected,isEnabled,isDisplayed,noThing,accept,dismiss;
}
